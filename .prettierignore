.claude/skills/
context/
# Generated by `supabase gen types typescript` and committed verbatim. The path was stale (pre-FSD
# `src/lib/…`), so prettier had in fact been rewriting it — meaning every regeneration produced a
# whole-file reformat diff on top of the real schema change. Ignored at its real path, the committed
# file is exactly what the generator emits and a regen diff shows only what the schema did.
src/shared/api/database.types.ts
supabase/seed.sql
poc/
# The frozen wire contract and its golden fixtures. The goldens are CANONICAL BYTES (compact, no
# whitespace, sorted keys) and are byte-compared by both test suites — lefthook runs `prettier --write`
# on staged `.json` with `stage_fixed: true`, which would re-indent them on every commit and break the
# comparison with nothing in CI to catch it (`pnpm format` is not a CI step). Same reason `poc/` is here.
contracts/
